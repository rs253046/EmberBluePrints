<div class="container">
	<div class="col-md-3">
		{{#link-to 'students.new' tagname="button" }}New{{/link-to}}
		<ul>
		  {{#each model as |student|}}
				<li>
				  {{#link-to 'students.student.show' student tagname="button" }}
				    {{student.student}}
				  {{/link-to}}
				</li>
			{{/each}}
		</ul>
	</div>
	<div class="col-md-9 v-divider">
		{{outlet}}
	</div>
</div>